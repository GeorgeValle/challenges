

const infoRouter = require('./info.router')
const randomRouter = require('./random.router')
const sessionRouter = require('./session.router')

module.exports = {
    infoRouter, randomRouter,sessionRouter
}